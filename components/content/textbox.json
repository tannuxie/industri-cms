{
  "connection": "default",
  "collectionName": "components_content_textboxes",
  "info": {
    "name": "textbox",
    "icon": "file-alt"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "component",
      "repeatable": false,
      "component": "content.text-content",
      "required": true
    },
    "layout": {
      "type": "enumeration",
      "enum": [
        "center",
        "vanster",
        "hoger"
      ],
      "default": "center",
      "required": true
    },
    "size": {
      "type": "enumeration",
      "enum": [
        "fullbredd",
        "tva_tredjedelar",
        "halv",
        "en_tredjedel",
        "en_fjardedel"
      ],
      "default": "halv",
      "required": true
    }
  }
}
